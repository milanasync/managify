import React, {Component} from 'react';

import CardOutLineComponent from './CardOutline';

const Card = props => {
  return <CardOutLineComponent {...props} ></CardOutLineComponent>;
};

export default Card;
